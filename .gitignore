.claude
.idea
.venv
__pycache__/
.ipynb_checkpoints/

# Jupyter — output 셀 박힌 큰 노트북이 repo 에 들어가는 것 방지.
# 의도적으로 트래킹할 노트북은 `git add -f <name>.ipynb` 로 강제 추가.
*.ipynb

# 모델 체크포인트 — 큰 binary, 학습마다 새로 생김. 별도 스토리지 사용.
*.pt

# Kaggle 제출 압축본 — scripts/train_submit 가 매번 생성하는 tar.gz.
submissions/
